# Optional. When set, /analyze uses Claude to refine heatmap peaks into
# hook-led clip boundaries (opens on a strong line, self-contained moment).
# Without it, HeatClip falls back to a transcript-snapped heuristic and still works.
ANTHROPIC_API_KEY=
